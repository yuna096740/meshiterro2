<div class="container">
  <div class="row no-gutters">
    <div class="col-md-5 col-sm-10 mx-2 mx-sm-auto text-center shadow-lg rounded px-0 my-5">
      <h5 class="text-dark bg-light py-4 mb-0">ユーザーログイン</h5>
      <div class="form-body p-4">
        <div class="mx-auto text-center">
          <p>ユーザー名と<br>パスワードを入力してください。</p>
        </div>
        <%= form_with model: @user, url: user_session_path do |f| %>
          <div class="input-group mb-3">
            <div class="input-group-prepend">
              <span class="input-group-text" aria-hidden="true"><i class="fas fa-user"></i></span>
            </div>
            <%= f.text_field:name,autofocus:true,class:"form-control",placeholder:"ユーザー名" %>
          </div>

          <div class="input-group mb-3">
            <div class="input-group-prepend">
              <span class="input-group-text" aria-hidden="true"><i class="fas fa-lock"></i></span>
            </div>
            <%= f.text_field:password,autocomplete:"off",class:"form-control",id:"inputPassword",placeholder:"パスワード" %>
          </div>

          <% if devise_mapping.rememberable? %>
            <div class="field">
              <%= f.check_box :remember_me %>
              <%= f.label :remember_me %>
            </div>
            <div class="field link-dark">
              <%= render "devise/shared/links" %>
            </div>
          <% end %>
          <%= f.submit "サインイン",class:"btn btn-lg btn-block btn-info mt-2" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
